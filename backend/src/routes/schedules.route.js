import express from "express";
import { requireAuth } from "@clerk/express";
// import {  } from "../controllers/schedules.controller.js";

const router = express.Router();

// public route

// protected routes

export default router;
